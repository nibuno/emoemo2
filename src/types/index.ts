export interface EmojiSettings {
  text: string;
  fontSize: number;
  fontFamily: string;
  textColor: string;
  backgroundColor: string;
}

export interface CanvasSize {
  width: number;
  height: number;
}